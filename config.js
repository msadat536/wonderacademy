// ============================================================
// Wonder Academy - configuration
// Edit this file, nothing else, to set up your family app.
// ============================================================
window.APP_CONFIG = {
  // App name shown on the home screen. Rename it to anything your kids like.
  APP_NAME: "Wonder Academy",

  // A short word only your family knows. It keeps your kids' profiles
  // grouped together in Supabase. Same value on every device.
  FAMILY_CODE: "our-family",

  // --- Supabase (optional but recommended for syncing across devices) ---
  // 1. Create a free project at https://supabase.com
  // 2. Run supabase.sql (in this folder) in the SQL editor
  // 3. Paste your Project URL and anon public key below
  // Leave both empty and the app still works, saving progress on the device only.
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: ""
};
